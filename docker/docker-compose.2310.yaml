version: "3"

services:

  test-setup:
    image: swiftly:23.10-test
    build:
      args:
        base_image: "swift:5.10-mantic"

  test:
    image: swiftly:23.10-test

  install-test-setup:
    image: swiftly:23.10-install-test
    build:
      args:
        base_image: "ubuntu:mantic"

  install-test:
    image: swiftly:23.10-install-test

  shell:
    image: swiftly:23.10-test
